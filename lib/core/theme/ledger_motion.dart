import 'package:flutter/material.dart';

/// Short and structural. No bounce.
abstract final class LedgerMotion {
  static const duration = Duration(milliseconds: 240);
  static const curve = Curves.easeInOut;
}
